---
layout: base.njk
title: Bài viết
description: Mẹo, hướng dẫn và công cụ giúp bạn chi tiêu thông minh hơn.
lang: vi
pagination:
  data: collections.postsVi
  size: 6
  alias: paginatedPosts
permalink: /blog/vi/{% if pagination.pageNumber > 0 %}{{ pagination.pageNumber + 1 }}/{% endif %}
---

{% set t = i18n.vi %}

<div class="blog-index-header">
  <h1 class="blog-index-title">{{ t.blogTitle }}</h1>
  <p class="blog-index-subtitle">{{ t.blogSubtitle }}</p>
</div>

{% if collections.tagListVi.length %}
<div class="blog-tag-filters">
  <a href="/blog/vi/" class="blog-tag-filter active">{{ t.all }}</a>
  {% for tag in collections.tagListVi %}
  <a href="/blog/vi/tags/{{ tag | slugify }}/" class="blog-tag-filter">{{ tag }}</a>
  {% endfor %}
</div>
{% endif %}

<div class="blog-post-list">
  {% for post in paginatedPosts %}
  <article class="blog-card">
    {% if post.data.featuredImage %}
    <a href="{{ post.url }}">
      <img class="blog-card-image" src="{{ post.data.featuredImage }}" alt="{{ post.data.title }}" loading="lazy">
    </a>
    {% endif %}
    <div class="blog-card-body">
      <div class="blog-card-meta">{{ post.date | readableDate("vi") }}</div>
      <h2 class="blog-card-title">
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </h2>
      <p class="blog-card-excerpt">{{ post.data.description or (post.content | excerpt) }}</p>
      {% if post.data.tags %}
      <div class="blog-card-tags">
        {% for tag in post.data.tags %}
          {% if tag != "post" %}
          <a href="/blog/vi/tags/{{ tag | slugify }}/" class="blog-tag">{{ tag }}</a>
          {% endif %}
        {% endfor %}
      </div>
      {% endif %}
    </div>
  </article>
  {% endfor %}
</div>

{% if pagination.pages.length > 1 %}
<nav class="blog-pagination" aria-label="Page navigation">
  {% if pagination.href.previous %}
  <a href="{{ pagination.href.previous }}" class="pagination-btn pagination-prev">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
    {{ t.previous }}
  </a>
  {% else %}
  <span class="pagination-btn pagination-prev disabled">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
    {{ t.previous }}
  </span>
  {% endif %}

  <div class="pagination-numbers">
    {% for pageEntry in pagination.pages %}
    {% set pageNum = loop.index0 %}
    {% if pageNum == pagination.pageNumber %}
    <span class="pagination-num active">{{ loop.index }}</span>
    {% else %}
    <a href="{{ pagination.hrefs[pageNum] }}" class="pagination-num">{{ loop.index }}</a>
    {% endif %}
    {% endfor %}
  </div>

  {% if pagination.href.next %}
  <a href="{{ pagination.href.next }}" class="pagination-btn pagination-next">
    {{ t.next }}
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
  </a>
  {% else %}
  <span class="pagination-btn pagination-next disabled">
    {{ t.next }}
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
  </span>
  {% endif %}
</nav>
{% endif %}
